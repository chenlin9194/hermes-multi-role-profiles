# Hermes 多角色协作系统 - 部署完成 ✅

## 🎯 部署状态
✅ **已成功部署** Hermes 多角色协作配置系统

## 📋 四个角色介绍

| 角色 | 职责 | 适用场景 |
|------|------|----------|
| **PM (项目经理)** | 目标拆解、任务路由、成果汇总 | 项目管理、任务分配、进度跟踪 |
| **SE (技术专家)** | 技术难题、代码分析、方案设计 | 技术攻关、代码审查、架构设计 |
| **Assistant (助理)** | 事务执行、信息检索、结构化输出 | 日常事务、信息整理、格式化输出 |
| **Writer (写手)** | 文档撰写、PPT生成、汇报材料 | 文档编写、报告制作、内容创作 |

## 🚀 使用方法

### 1. 命令行切换角色
```bash
# 切换到技术专家
export PATH="$HOME/.hermes/bin:$PATH"
hermes-profile se

# 切换到项目经理
hermes-profile pm

# 切换到助理
hermes-profile assistant

# 切换到写手
hermes-profile writer

# 查看可用角色
hermes-profile
```

### 2. 对话中指定角色
直接在对话中说明：
- "让 SE 分析这个技术问题"
- "让 PM 帮我规划这个项目"
- "让 Assistant 整理这些信息"
- "让 Writer 帮我写个文档"

### 3. 委派任务
PM 角色可通过 `delegate_task` 将任务分发给其他角色。

## 📁 系统结构
```
~/.hermes/
├── SOUL.md              # 当前激活的角色配置
├── config.yaml          # 核心配置
├── profiles/            # 多角色配置目录
│   ├── pm/SOUL.md
│   ├── se/SOUL.md
│   ├── assistant/SOUL.md
│   └── writer/SOUL.md
├── memories/            # 长期记忆（共享）
├── skills/              # 自定义技能（共享）
└── bin/                 # 工具脚本
    └── hermes-profile   # 角色切换脚本
```

## 💡 核心特性

1. **数据共享**：所有角色共享 `memories/` 和 `skills/` 目录
2. **按需调度**：PM 根据任务性质自动路由任务
3. **人格隔离**：每个角色有独立的 `SOUL.md` 定义

## 🔧 环境配置

如果命令行切换不生效，请确保 PATH 配置正确：
```bash
# 临时生效（当前会话）
export PATH="$HOME/.hermes/bin:$PATH"

# 永久生效（添加到 ~/.bashrc）
echo 'export PATH="$HOME/.hermes/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

## 🎉 开始使用

现在您可以开始使用多角色协作系统了！试试切换到不同角色，体验专业化的 AI 助手服务。